# DT / DX 文件格式（地听自有格式）V1

> 2026-08-24 · 劳雷GPR · `*.DT` = 数据文件(对应 DZT), `*.DX` = 处理文件(对应 DZX)

## 1. V1 定义：与 DZT/DZX 完全兼容

**V1 阶段 DT/DX 与 DZT/DZX 字节级完全相同，仅后缀不同。**

| 自有格式 | 对应 RADAN 格式 | 内容 |
|---|---|---|
| `*.DT` | `*.DZT` | 1024字节文件头 + 16bit 采样数据 |
| `*.DX` | `*.DZX` | XML 处理记录(MarkGroup/LayerGroup/TargetGroup/BinaryData) |

- 转换 = **字节复制 + 改后缀**，无任何内容修改（含 mtime 不变语义由读写方维持）。
- RADAN 可直接把 `.DT` 改名为 `.DZT` 使用（反之亦然），100% 互操作。

## 2. 配对规则（本软件读写逻辑，v1.0.142 起）

源数据文件的伴随处理文件查找顺序（`dzxPairPath()`）：

| 源文件 | 读取优先序 | 写入目标 |
|---|---|---|
| `xxx.DT` | `xxx.DX` → `xxx.DZX` | `xxx.DX` |
| `xxx.DZT` | `xxx.DZX` → `xxx.DX` | `xxx.DZX` |

即：跟随源文件后缀（自有配自有，RADAN配RADAN）；读取时另一后缀作为回退（兼容混合场景）。

## 3. 格式转换功能（品牌菜单 → 格式转换）

- 源 `.DZT` → 输出同名 `.DT`，**同时若存在同名 `.DZX` 一并转为 `.DX`**
- 源 `.DZX` → 输出同名 `.DX`
- 输出目录可选，转换后可自动在软件中打开
- 目标格式下拉中的 SEG-Y(.SGY)/ASCII(.CSV) 为后续版本预留

## 4. 后续版本扩展方向（不破坏 V1 兼容）

- DT 头保留区写入自有标识（如 magic/版本号），未识别时仍按 DZT 兼容解析；
- DX 增加自有节点（本软件私有信息），RADAN 未知节点按其规则忽略；
- 压缩/加密、采集元数据扩展等 —— 均以"V1 文件可被新版本读取"为前提。
