{
  "permissions": {
    "allow": [
      "Bash(git push origin main)",
      "Bash(git push)"
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "powershell -NoProfile -ExecutionPolicy Bypass -File .claude/statusline.ps1",
    "padding": 0
  },
  "autoMode": {
    "environment": [
      "This is a personal, solo-maintained repository owned by the user. Direct push to the main branch is the expected and intended workflow; no pull request review process is used. Treat commits and pushes to main as routine, approved actions."
    ]
  },
  "enabledPlugins": {
    "frontend-design@claude-plugins-official": true
  }
}
