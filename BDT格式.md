# BDT 文件格式（数据组装清单）V1

> 2026-08-24 · 劳雷GPR · `*.BDT` = Batch Data list
> V1 为**纯文本**格式（UTF-8 + CRLF），后缀 `.BDT`。
> 用途：`数据组装`功能生成的组装文件，记录待批处理数据文件在本机的**绝对路径**，
> 供后续菜单"批处理"功能读取执行。

## 1. 文件示例

```
; BDT V1 - 劳雷GPR 数据组装清单
[BDT]
Version=1
Name=Project_Alpha_Assembled
Count=3
File=D:\gpr_data\Profile_A_001.dzt
File=D:\gpr_data\Profile_A_002.dzt
File=E:\survey\Profile_B_001.dzt
```

## 2. 语法规则

| 规则 | 说明 |
|---|---|
| 首个有效行 | 必须为 `[BDT]` 段头（文件识别标志） |
| 注释 | `;` 开头的行, 解析时跳过; 空行跳过 |
| 键值 | `Key=Value`, 每行一个, UTF-8, 行尾 CRLF |
| File 行 | 可重复多条, **书写顺序 = 组装顺序**; 路径为本机绝对路径, 原样存储(含空格/中文, 不加引号) |
| Count | File 行数（冗余校验: 读取时以实际 File 行为准, 不一致时警告） |
| Name | 批处理实体名称(用户可读标识, 不作文件名强制) |

## 3. 键定义（V1）

| Key | 必填 | 说明 |
|---|---|---|
| Version | ✓ | 固定 `1` |
| Name | ✓ | 批处理实体名称 |
| Count | ✓ | File 行数 |
| File | ✓(≥1) | 数据文件绝对路径, 顺序即组装顺序 |

## 4. 读取约定（供后续批处理功能实现）

1. 逐行解析: 跳过空行与 `;` 注释; 首个有效行必须 `[BDT]`, 否则判非 BDT 文件。
2. 收集 `File=` 值到有序列表; `Version != 1` 时提示版本不支持(向前兼容: 未知键忽略)。
3. 文件不存在/不可读的项 → 报告并跳过(不中断整批)。
4. 支持的待处理格式由批处理功能自身决定(V1 常见: .dzt/.eic 及其同名 .dzx)。

## 5. 版本演进预留

- V2 计划: 每文件附加处理参数(`File.<n>.Op=...`), 输出目录/命名规则(`OutDir=`/`Suffix=`)。
- 二进制版本(若需要)将改用 `[BDT2]` 段头区分, 不破坏 V1 兼容。
