# 业务云接入授权云 · 开发接口文档

> 版本 v1.0 · 2026-09-02 · 适用于业务云(云端高级业务计算服务)开发工程师
> 授权云 = License 授权服务(PHP, 唯一授权真相源) · 设计总纲见 `specs/license管理需求.md`

---

## 1. 架构与信任模型(必读)

```
Windows客户端(depro.exe)                授权云(本文档对象)
   │ ①请求云端高级功能                ┌────────────────────┐
   │  携带 licenseKey + deviceId      │  唯一授权真相源      │
   ▼                                 │  MySQL licenses 绑定 │
┌──────────────┐   ②服务端二次校验    │  RSA私钥签发凭证     │
│   业务云      │ ──────────────────▶ │  POST /api/verify   │
│ (你的服务)    │ ◀────────────────── │  {valid,featureMask}│
└──────────────┘   ③valid=true才服务  └────────────────────┘
```

**核心规则(来自安全设计, 不可绕过)**:
1. **客户端不可信**。客户端上报的"我已激活"一律无效, 业务云每笔业务请求都必须向授权云做服务端二次校验。
2. **业务云不存储授权数据**, 不判断授权逻辑, 只透传 `licenseKey + deviceId` 给授权云并按返回结果放行/拒绝。
3. ❌ **禁止业务云直连授权云 MySQL**, 一律走本 HTTPS 接口。
4. ❌ **`X-Server-Secret` 严禁出现在 Windows 客户端、前端页面、git 仓库、日志中**——它是服务器之间的身份凭证, 泄露等于别人可冒充你的服务查询授权。

## 2. 接入前提

| 项 | 说明 |
|---|---|
| 接口地址(测试环境) | `https://www.sxfpga.cn/license/api/verify.php` |
| 服务间密钥 | `X-Server-Secret` 一个值, **向授权管理员索取**(微信/邮件线下交付); 每个业务云独立发放 |
| 传输 | 仅 HTTPS(证书 DigiCert DV, 有效期至 2026-09-24, 到期由管理员续) |
| 可选加固 | 业务云固定公网 IP 报给管理员 → 授权云侧加 IP 白名单 |

## 3. 接口规范 · 授权校验

### 请求
```
POST /license/api/verify.php
Content-Type: application/json
X-Server-Secret: <你的服务间密钥>

{"licenseKey": "DT-XXXXX-XXXXX-XXXXX", "deviceId": "<64位十六进制机器指纹>"}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| licenseKey | string | 是 | 客户端透传的授权码, 标准格式 `DT-XXXXX-XXXXX-XXXXX`(20位含连字符, 字母表不含0/O/1/I); 大小写不敏感 |
| deviceId | string | 是 | 客户端透传的机器指纹, 64 位小写十六进制(SHA256) |

### 响应(JSON, HTTP 200)
```json
{"code": 0, "msg": "ok", "valid": 1, "featureMask": 1}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| code | int | 0=请求处理成功(≠授权有效!); 1=请求本身失败(鉴权/参数) |
| valid | int | **授权判定**: 1=该授权码有效且绑定此设备; 0=无效(未激活/绑了别的设备/已作废/不存在) |
| featureMask | int | 功能位掩码。valid=0 时恒为 0。bit0=AI分析模块; 业务云按自己的功能位判断 |

### 错误码(code=1)
| err | 含义 | 业务云处理 |
|---|---|---|
| ERR_CLIENT | X-Server-Secret 缺失/错误 | 检查密钥配置, 不要重试轰炸 |
| ERR_METHOD | 非 POST | 修改请求方法 |
| ERR_PARAM | 缺 licenseKey/deviceId | 检查客户端上送字段 |
| ERR_KEY_FORMAT / ERR_DEVICE_FORMAT | 字段格式不符 | 视为未授权, 拒绝服务 |
| ERR_THROTTLE | 触发限速 | 退避重试(见 §6) |

### curl 示例
```bash
curl -s -X POST https://www.sxfpga.cn/license/api/verify.php \
  -H "Content-Type: application/json" \
  -H "X-Server-Secret: <你的密钥>" \
  -d '{"licenseKey":"DT-L9T35-CLBZ4-EJH6F","deviceId":"0123456789abcdef...共64位"}'
```

## 4. 业务云侧判定伪代码

```text
req = 客户端请求(licenseKey, deviceId, 业务参数)
if licenseKey 或 deviceId 缺失/格式明显不对:
    return 401 未授权

resp = POST 授权云verify (X-Server-Secret)      # 见 §5 缓存策略
if 请求失败(超时/5xx/非JSON):
    return 503 服务暂不可用            # 默认拒绝; 如业务允许降级须与管理员书面确认
if resp.code != 0:
    return 503                          # 鉴权/参数问题, 记日志告警
if resp.valid != 1:
    return 401 授权无效                  # 提示客户端重新激活
if !(resp.featureMask & 你服务的功能位):
    return 403 未订阅该功能
# —— 到这里才执行真正的云端业务计算 ——
```

## 5. 缓存策略(可选, 推荐模式B)

| 模式 | 做法 | 作废生效延迟 | 授权云压力 |
|---|---|---|---|
| A 实时 | 每业务请求都校验 | 即时 | 最高 |
| **B 短缓存(推荐)** | 以 `licenseKey+deviceId` 为 key 缓存 verify 结果, **TTL ≤ 300 秒** | 最多=TTL | 低 |

- 缓存只存 `valid+featureMask` 两个字段, TTL 到期必须回源。
- 后台"作废/解绑"操作后, 已缓存的放行最多再持续一个 TTL——这是 spec 允许的权衡, 不要为了即时性去掉缓存上限去直连数据库。

## 6. 超时/重试/限速

- 建议连接+读取总超时 **3~5 秒**; 超时按失败处理(§4)。
- 授权云侧对**激活/解绑**接口有 IP 失败限速(15分钟≥10次锁); **verify 不限速**, 但请业务云侧做好自身重试上限(如 2 次指数退避), 避免雪崩。
- 业务云收到 `ERR_THROTTLE` 说明同 IP 失败过多, 停止重试并检查是不是密钥配错。

## 7. 客户端侧配合(业务云需知)

- 客户端激活后本地持有签名凭证, 调业务云接口时**明文上送** `licenseKey` + `deviceId`(64hex)。业务云只需透传, 无需也不应解析凭证内容。
- 日志脱敏建议: `licenseKey` 记录脱敏形式(`DT-XXXXX-*****-*****`, 保留前8位), deviceId 保留前12位。
- 客户端断网时只靠本地凭证运行**本地**功能; 云端功能必须联网走到你这里, 不会出现"离线调用云端"的诉求。

## 8. 安全规范

1. `X-Server-Secret` 存放于业务云的环境变量/密钥管理服务, 不进代码仓库, 不打进镜像层。
2. 密钥轮换: 管理员可随时更换, 提前 24h 通知, 双密钥并行窗口 1 小时。
3. 所有请求走 HTTPS; 业务云侧校验授权云证书(默认行为, 勿关闭证书验证)。
4. 发现疑似泄露立即报管理员作废旧密钥。

## 9. 联调测试

1. 向管理员申请: ①测试 `X-Server-Secret` ②测试授权码若干(后台生成, 客户名标"联调")。
2. 自造 deviceId: 任意 64 位十六进制字符串即可(如 `aa`×32)。
3. 联调用例: 有效码+匹配设备→valid=1 | 有效码+任意其他设备→valid=0 | 不存在码→valid=0 | 后台作废后再查→valid=0(若用缓存, 等TTL)。
4. 激活动作由 Windows 客户端完成(或让管理员在后台把测试码绑到你造的 deviceId 上), 业务云**不要**调用 activate/unbind。

## 10. 路线图(未实现, 勿依赖)

- **JWT 短期令牌模式**: 客户端激活时获取短期签名 Token, 业务云本地验签免每次网络查询; 缺点=后台作废需等 Token 到期。当前版本请一律走 verify 网络校验。
- 独占生产环境上线时会更换域名/密钥, 届时本文档所有地址与密钥同步更新并升版。
